Additionally, we will verify whether this parameter is effectively recycling the inactive sessions by temporarily disabling the idle-timeout setting from the database. This will allow us to observe if the feature successfully operates for a 10-minute period—whether the PID is being cleared—or if there's an issue with uWSGI holding onto the connection. We'll also look into finding any other related issues that might arise.
