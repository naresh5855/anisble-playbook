The file upload to /gcp/objects is handled directly by the UI code, and it appears that LBaaS within the UI component is causing the slow upload speeds. It is possible to bypass LBaaS by using NGINX as a reverse proxy on the same port that LBaaS is configured to listen on. However, this could lead to unforeseen issues that may arise.
