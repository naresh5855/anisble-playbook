The current upstream timeout is configured to 900 seconds, which should be sufficiently long to handle most backend operations without causing timeouts. Regarding the API that returns a 200 success response, could you please specify which API endpoint is involved? 
